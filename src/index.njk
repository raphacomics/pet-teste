---
title: PraPet Teste
layout: 'base.njk'
---
    

    <div class="container">
        <h1>Produtos</h1>
        
        <div class="prod-list">
 
            {%- for prods in collections.prods -%}

                <div class="produto-card">
                    <h1>{{ prods.data.nome }}</h1>
                    <span>{{ prods.data.preco }}</span>
                    <p>{{ prods.data.descricao }}</p>
                   <button><a href="{{ prods.url }}"> Comprar </a></button>
                </div>

            {%- endfor -%}
    </div>

    </div>

